export const sendWhatsappNotification = async ({ studentName, phoneNumber, message }: any) => {
    // Simulated WhatsApp Logic (could integrate Twilio here)
    console.log(`📲 Sending WhatsApp to ${studentName} (${phoneNumber}): ${message}`);
  };
  