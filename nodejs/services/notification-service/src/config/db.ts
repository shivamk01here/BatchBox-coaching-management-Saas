export const connectToDB = async () => {
    try {
      console.log('✅ Notification service DB connected (mock)');
    } catch (err) {
      console.error('❌ Notification DB failed:', err);
    }
  };
  