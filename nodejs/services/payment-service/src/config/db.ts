export const connectToDB = async () => {
    try {
      // Replace with your MongoDB or SQL connection logic
      console.log('✅ Connected to DB (mock)');
    } catch (err) {
      console.error('❌ DB Connection Failed:', err);
    }
  };
  